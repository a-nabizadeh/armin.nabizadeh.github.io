// Get references to the sections
const researchSection = document.getElementById('research');
const publicationsSection = document.getElementById('publications');
const cvSection = document.getElementById('cv');

// Add event listeners to the sections
researchSection.addEventListener('click', () => {
  // Action to perform when Research section is clicked
  console.log('Research section clicked');
});

publicationsSection.addEventListener('click', () => {
  // Action to perform when Publications section is clicked
  console.log('Publications section clicked');
});

cvSection.addEventListener('click', () => {
  // Action to perform when CV section is clicked
  console.log('CV section clicked');
});
